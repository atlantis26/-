1、 添加role角色表，分为管理员manager和普通用户user两种用户，不同用户角色具有不同的操作权限；
    > 管理员可以通过yml配置文件进行用户、主机等资源信息的创建和录入以及对有权限的主机进行登录操作：
            1、新增主机                     2、新增主机组
            3、新增本地系统用户             4、新增远程登录账号
            5、新增主机映射关系             6、通过主机列表选择主机
            7、通过主机组选择主机

    > 普通用户具有对有权限的主机的登录、命令执行等操作：
            1、通过主机列表选择主机         2、通过主机组选择主机


2、新增redis存储，使用列表缓存用户的主机命令执行记录日志；当日志条数达到limit(10条)时，取出redis缓存中的日志记录，并写入到mysql数据库

