select版本ftp系统，是在TreadTCPServer版本上修改，主要功能未改版，主要修改了socket服务端实现由多线程处理请求，修改为select的IO多路复用；
文件的上传下载，分多次数据块传输，即多次socket请求下载同一文件，每次下载部分文件内容；
通过定义状态码来判断是否控制文件内容的传输：请求响应状态code=201表示未完成文件的传输，code=200标示完成文件传输
每次文件下载请求的数据组成： 请求json参数的二进制字符串 +  二进制分隔符 + 部分文件内容





ftp_server服务端：
    1.实现多用户登录
    2.用户自己的家目录，只能访问自己的家目录
    3.注册用户时进行磁盘配额，设置默认配额
    4.上传文件校验用户剩余配额是否足够；
    5.上传、下载文件以及断点续传完成时，校验MD5值是否一致
    5.用户自己的家目录下随意切换目录
    6.实现对如下命令执行的对应的方法
          mkdir    当前路径下创建一个目录
          remove   删除文件或目录及其子文件、目录
          cd       切换目录
          ls       查看当前路径下的文件、目录
          put      上传文件
          get      下载文件(支持完整或断点续传文件)
          pwd      当前所在路径
          quota    用户配额信息
          help     帮助指令，可查询所有可执行命令的使用语法详细说明
    7.help查询某命令的语法详细信息，数据取于对应函数的doc描述


ftp_client客户端：
    1.用户注册、登录时对密码进行MD5加密后传输给后台
    2.上传、下载文件时，打印进度条,校验MD5值
    3.实现未登录与已登录两个视图，登录后进入用户ftp shell视图
    4.在用户登录shell后，可执行本系统支持的各种命令操作，执行help命令可查询到所有命令的详细
    5.支持文件下载断点续传get命令操作


