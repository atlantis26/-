ftp_server服务端：
    1.实现多用户登录（TreadTCPServer）
    2.用户自己的家目录，只能访问自己的家目录
    3.注册用户时进行磁盘配额，设置默认配额
    4.上传文件校验用户剩余配额是否足够；
    5.上传、下载文件以及断点续传完成时，校验MD5值是否一致
    5.用户自己的家目录下随意切换目录
    6.实现对如下命令执行的对应的方法
          mkdir    当前路径下创建一个目录
          remove   删除文件或目录及其子文件、目录
          cd       切换目录
          ls       查看当前路径下的文件、目录
          put      上传文件
          get      下载文件(支持完整或断点续传文件)
          pwd      当前所在路径
          quota    用户配额信息
          help     帮助指令，可查询所有可执行命令的使用语法详细说明
    7.help查询某命令的语法详细信息，数据取于对应函数的doc描述


ftp_client客户端：
    1.用户注册、登录时对密码进行MD5加密后传输给后台
    2.上传、下载文件时，打印进度条,校验MD5值
    3.实现未登录与已登录两个视图，登录后进入用户ftp shell视图
    4.在用户登录shell后，可执行本系统支持的各种命令操作，执行help命令可查询到所有命令的详细
    5.支持文件下载断点续传get命令操作


遗留问题：ctrl+c中止文件下载时，本来设计为下载中止，但是不退出shell，但是无法很好解决，缓存中残余文件内容数据的问题；
所以暂时修改为ctrl+c中止命令时，用户也退出登录shell（关闭当前socket连接，重启新scoket连接）；用户需要重新登录shell

