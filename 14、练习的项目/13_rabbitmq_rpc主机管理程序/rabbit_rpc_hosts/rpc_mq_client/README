rabbit_client:
    1、声明一个用于接收返回数据的队列，队列名通过conf内的配置文件设置；不采用随机队列而是声明一个持久化队列
       因为创建的随机队列在创建它的连接断开时，该队列会自动删除，可能导致消息丢失；
    2、客户端支持并发在多个server上执行命令, 单个server host与单个MQ队列一一对应（客户端设置的服务端队列名称必须准确）；
        比如：MQ_QUEUE_DICT = {"127.0.0.1": "server_queue_1",
                              "192.168.2.105": "server_queue_2"}
    3、发送与接收返回数据的队列持久化、消息持久化；
    4、支持3个命令：
        1）run命令:
            举例：run "df -h" --hosts 192.168.2.100,192.168.2.101
            命令执行结果：
                task_id: 1234482993
                task_id: 4565662223

            参数中多个ip地址以逗号连接，如上分别在两个服务器上异步执行命令，非阻塞任务执行，
            返回task_id，可通过task_id查询异步执行结果

        2)check_task命令：
            举例：check_task 123448299
            命令执行结果： 返回"df -h"命令分别

            通过执行task_id,查询异步执行结果，如果执行完成后，则返回执行结果，未执行完成，则返回None值

        3)show命令：
            举例： show
            命令执行结果： 展示系统全部录入的已部署rabbit_server的host地址